# Qiekn's Notes

This is the enigmash clone project. the original game (./refs/enigmash-original/game.txt) is made by jacklance using PuzzleScript (./refs/jacklance-puzzlescript/)

This project using c++23 & raylib, the structure follows ../baba/ & ../card/

- layer stack
- scene manager
- imgui viewport & docking

"p.i." and "sqrt3" is short for JackLance

## Code style

basically using google c++ code style, but line witdh can be 120 if necessary.

section comment use this format

```cpp
- -----------------------------------------------------------------------------: context
```

## Git

git commit message must be simple: single-line `type(scope): summary`, no body paragraph.

# CLAUDE's Notes
